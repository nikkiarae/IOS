import UIKit

// ARRAYS
// Array index starts at 0 (first item is 0)
// The type must stay the same

var array = [1, 2, 3, 4, 5, 6, 7, 8]
let test = array[1]

// Declare Array type
var array2: [String] = []
